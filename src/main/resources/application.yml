spring:
  application:
    name: accounts

  config:
    import: "optional:configserver:http://localhost:8071/"

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: rida
    password: admin000pwd

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-database

  profiles:
    active: qa   # change from prod if using H2

server:
  port: 9090

management:
  endpoints:
    web:
      exposure:
        include: "*"